name: Wishlink Converter

on:
  workflow_dispatch:
    inputs:
      product_link:
        description: "Product link to convert"
        required: true

jobs:
  convert:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm

      - name: Install dependencies
        run: npm ci

      # 👇 FIXED: Grants permission to the binary before running it
      - name: Install Playwright Browsers
        run: |
          chmod +x ./node_modules/.bin/playwright
          ./node_modules/.bin/playwright install chromium

      - name: Convert link
        env:
          WISHLINK_STORAGE: ${{ secrets.WISHLINK_STORAGE }}
        run: |
          node convert.js "${{ github.event.inputs.product_link }}"

      - name: Upload result
        uses: actions/upload-artifact@v4
        with:
          name: wishlink
          path: wishlink.json
